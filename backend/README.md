# Backend — Manufacturing Equipment Monitoring System (FastAPI)

FastAPI backend that stores equipment, ingests sensor readings, generates alerts,
and computes window-based health scoring.

## Requirements
- Python 3.12+
- venv activated
- Dependencies installed

## Setup
```bash
cd backend
python -m venv venv
source venv/bin/activate
pip install -r requirements.txt
